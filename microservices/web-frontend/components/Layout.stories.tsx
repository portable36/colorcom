import React from 'react';
import Layout from './Layout';

export default { title: 'Layout' };

export const Default = () => (
  <Layout>
    <div style={{ padding: 20 }}>Hello from Storybook</div>
  </Layout>
);
